import AppGradientBackground from '@/components/appGradientBackground';
import React from 'react';
import { Text, View } from 'react-native';

const Profile = () => {
  return (
    <AppGradientBackground colors={["#298634","#996400"]}/>
  );
};

export default Profile;
